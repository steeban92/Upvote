trigger:
- main

stages:
- stage: build
  jobs:
  - job: build 
    pool:
      vmImage: ubuntu-latest
    steps: 
    - task: NodeTaskRunnerInstaller@0
      inputs:
        nodeVersion: '16'  # Updated Node.js version to a more recent LTS version

    - task: Npm@1
      displayName: 'install npm'
      inputs:
        command: 'install'

    - task: Npm@1
      displayName: 'build npm'
      inputs:
        command: 'custom'
        customCommand: 'run build'
